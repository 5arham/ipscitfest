# Generated output. `node build.mjs` renders ./site from ./data + ./assets, so
# the rendered pages are a build artifact, not source. The generator and the
# data file are the project.
/site/

# Editor / tooling
.claude/
.DS_Store
Thumbs.db
.vscode/
.idea/
node_modules/
